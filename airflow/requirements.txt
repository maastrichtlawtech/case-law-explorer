# First-party dependencies of the ETL DAGs, installed on top of the official
# apache/airflow base image (see Dockerfile). Airflow itself, its providers,
# and their transitive stack (Flask, SQLAlchemy, requests, ...) ship with the
# base image -- do not re-pin them here. When upgrading Airflow, re-check these
# pins against the matching constraints file:
# https://raw.githubusercontent.com/apache/airflow/constraints-<version>/constraints-3.11.txt

# caselaw source extractors
cellar_extractor==2.0.0
echr_extractor==1.2.1
rechtspraak_extractor==1.5.2
rechtspraak_citations_extractor==1.2.0

# data processing
pandas==2.3.3
lxml==6.0.2
python-dateutil==2.9.0.post0

# LIDO RDF/turtle processing
pyoxigraph==0.5.6
rdflib==7.6.0

# infrastructure
psycopg2-binary==2.9.11
python-dotenv==1.2.2
